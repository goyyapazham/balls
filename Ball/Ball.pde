class Ball extends PShape {
  
  //LOWKEY HIGHKEY HAVE NO IDEA WHAT'S GOING ON
  /* nala lowkey high key has no idea what's going on */
  
  Ball(int xstart, int ystart, int xrad, int yrad) {
    ellipse(xstart, ystart, xrad, yrad);
  }
  
}